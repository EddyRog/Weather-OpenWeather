# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Weather-OpenWeather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Weather-OpenWeather
  pod 'SwiftyJSON'

  target 'Weather-OpenWeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Weather-OpenWeatherUITests' do
    # Pods for testing
  end

end
